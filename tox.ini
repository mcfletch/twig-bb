[tox]
# These environments resolve the game's dependencies from an index, so they
# build only once the versions `[project] dependencies` names are published --
# between releases OpenGLContext's and PyOpenGL's exist in this checkout and
# nowhere else, and uv answers "not available" whatever `pip_pre` says. What
# holds this project to its declared gates in the meantime is
# `tools/preflight.py` in the workspace above, which installs every checkout
# into one environment and runs ruff, mypy and the suite there.
envlist = py310, py311, py312, py313, py314
isolated_build = True

[testenv]
extras = dev
# An editable install rather than a built wheel: parts of the suite read files
# that live beside the package in the source tree, not inside it -- the
# checked-in ``NOTICES.md`` licence manifest (``test_notices``) and the sample
# maps under ``tmp/`` -- so they must see the repository layout.
package = editable
# The core dependencies (OpenGLContext 3.0.0a, pyopengl 4.0.0a, omi_audio
# 0.2.0a) are published only as pre-releases, which uv/pip skip by default;
# without this the resolver reports them "not available" and the env fails to
# build.  pip_pre is honoured by both the pip and the uv installers.
pip_pre = true
commands = pytest {posargs}
